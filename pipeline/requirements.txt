# Training dependencies for QLoRA fine-tuning a local code model on a CUDA GPU.
# Install on the pod (PyTorch 2.x / CUDA 12 image; torch is preinstalled there).
#
#   pip install -r requirements.txt
#
# cohere2_moe is a brand-new architecture and REQUIRES transformers >= 5.8.0.
# If that release isn't out yet, install transformers from source instead:
#   pip install git+https://github.com/huggingface/transformers.git

transformers>=5.8.0
trl
peft
datasets
accelerate
bitsandbytes
wandb

# Optional but preferred (fastest, lowest VRAM). train.py falls back to
# transformers + PEFT if Unsloth can't load the cohere2_moe architecture.
unsloth
